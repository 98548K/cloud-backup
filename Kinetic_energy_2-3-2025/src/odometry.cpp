//Declares variables at beginning
/*
#include "vex.h"
#include "cmath"
double HORIZONTAL_WHEEL = sideTracking.position(turns) * (wheelRad * 2) * M_PI;
double VERTICAL_WHEEL = frontTracking.position(turns) * (wheelRad * 2) * M_PI;

double X;
double Y;

double deltaX;
double deltaY;

double startPos;

double robotWidth = 15;
double setupTurnCoordinates;
double setupDriveCoordinates;

void getY() {
    return cos(Inertial1.heading(deg)) = 72/VERTICAL_WHEEL;
}
void getX() {
    return cos(Inertial1.heading(deg)) = 72/HORIZONTAL_WHEEL;
}

//Creates the task for constantly tracking the position



int positionTracking() {
    while (true) {
        
        HORIZONTAL_WHEEL = sideTracking.position(turns) * (wheelRad * 2) * M_PI;
        VERTICAL_WHEEL = frontTracking.position(turns) * (wheelRad * 2) * M_PI;

        Y = getY;
        X = getX;
    }
}


//make functions for odometry movement
*/